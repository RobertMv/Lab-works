import math
#считываем n и x, вслучае некорректного ввода просим ввести числа еще раз
n = int(input("Введите n: "))   
while n < 0:
    print(" Число n должно быть больше 0 ")
    n = int(input(" Введите число n еще раз: "))
    
x = float(input("Введите x: "))
while math.isinf(math.fabs(x)) == True:
    print(" Число x по модулю должно быть меньше бесконечности ")
    x = float(input(" Введите число x еще раз: "))
    
#задаем начальные значения числителя и знаменателя
a = 1 #числитель
b = 1 #знаменатель
summa = 0 # сумма последовательности

# в цикле считаем новый член последовательности
# увеличивая предыдущий на их разницу и складываем
for i in range(1, n+1):
    chlenPosl = a/b
    summa = summa + chlenPosl
    a = a * (-(x**2))
    b = b*(i+1)*(i+2)

# печатаем сумму
print("Сумма ряда: ", summa)
